<%= form_for current_user.active_relationships.build, html: {class: "col-md-12"}, remote: true do |f| %>
  <div><%= hidden_field_tag :followed_id, @user.id %></div>
  <%= f.submit t("follow"), class: "btn btn-primary" %>
<% end %>
